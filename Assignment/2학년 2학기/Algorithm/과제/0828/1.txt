#include <stdio.h>

int operation(int, int, int*, int*, int*, double*);

int main() {

	int num1, num2, plus, minus, multi;
	double div;

	printf("µÎ ¼ö¸¦ ÀÔ·ÂÇÏ¼¼¿ä : ");
	scanf("%d %d", &num1, &num2);

	operation(num1, num2, &plus, &minus, &multi, &div);

	printf("µÎ °ªÀÇ µ¡¼À °á°ú : %d\n", plus);
	printf("µÎ °ªÀÇ »¬¼À °á°ú : %d\n", minus);
	printf("µÎ °ªÀÇ °ö¼À °á°ú : %d\n", multi);
	printf("µÎ °ªÀÇ ³ª´°¼À °á°ú : %f\n", div);

	return 0;
}

int operation(int a, int b, int* plus, int* minus, int* multi, double* div) {
	*plus = a + b;
	*minus = a - b;
	*multi = a * b;
	*div = (double)a / b;

	return 0;
};